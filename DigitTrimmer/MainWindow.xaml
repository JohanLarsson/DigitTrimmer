<Window x:Class="DigitTrimmer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:autoRowGrid="http://gu.se/AutoRowGrid"
        xmlns:digitTrimmer="clr-namespace:DigitTrimmer"
        Title="MainWindow"
        Width="525"
        Height="350">
    <Window.DataContext>
        <digitTrimmer:ViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <GroupBox Header="Settings">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0"
                           Grid.Column="0"
                           Text="Digits" />
                <TextBox Grid.Row="0"
                         Grid.Column="2"
                         Text="{Binding Digits}" />
                <TextBlock Grid.Row="1"
                           Grid.Column="0"
                           Text="Size" />
                <TextBox Grid.Row="1"
                         Grid.Column="2"
                         Text="{Binding Size,
                                        TargetNullValue=''}" />
                <TextBlock Grid.Row="2"
                           Grid.Column="0"
                           Text="Shift to origin" />
                <CheckBox Grid.Row="2"
                          Grid.Column="2"
                          IsChecked="{Binding ShiftToOrigin}" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Input">
            <TextBox x:Name="InTb" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" />
        </GroupBox>
        <GroupBox Grid.Row="2" Header="Output">
            <TextBox x:Name="OutTb"
                     IsReadOnly="True"
                     Text="{Binding Output,
                                    Mode=OneWay,
                                    UpdateSourceTrigger=PropertyChanged}" />
        </GroupBox>
    </Grid>
</Window>







